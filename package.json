{
  "name": "ha-light-3d",
  "version": "1.0.0",
  "description": "用于9楼B区的灯光控制DEMO",
  "main": "main.js",
  "scripts": {
    "package-win": "electron-packager . MyApp --platform=win32 --arch=x64 --out=release-builds --overwrite",
    "package-pi": "electron-packager . MyApp --platform=linux --arch=armv7l --out=release-builds --overwrite",
    "start": "electron .",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Qqdyta",
  "license": "MIT",
  "devDependencies": {
    "electron": "^23.1.3",
    "electron-packager": "^17.1.2"
  },
    "dependencies": {
      "electron": "^23.1.3"
    }
}
